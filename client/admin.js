const $=s=>document.querySelector(s);
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&','<':'<','>':'>','"':'"',"'":'&#39;'}[c]));
function photoMarkup(url,title){return url?`<img class="maintenance-photo" src="${esc(url)}" alt="${esc(title)}" loading="lazy">`:''}
function badge(v){return `<span class="badge ${(v||'').toLowerCase()}">${esc((v||'').replace('_',' '))}</span>`}
async function api(url,options){const r=await fetch(url,{headers:{'Content-Type':'application/json',...(options?.headers||{})},...options});if(r.status===401){location.href='/login';throw Error('Your session has expired.');}if(!r.ok){const e=await r.json().catch(()=>({}));throw Error(e.error||'Something went wrong.');}return r.status===204?null:r.json()}
function fail(e){$('#error').textContent=e.message;$('#error').hidden=false}function clearFail(){$('#error').hidden=true}
function money(v){return v==null?'-':new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(Number(v))}
function isLate(row){const today=new Date();today.setHours(0,0,0,0);return (row.charge.status==='DUE'||row.charge.status==='OPEN')&&new Date((row.dueDate||row.charge.periodStart)+'T00:00:00')<today}
function route(){const raw=(location.hash.slice(1)||'overview').split('?')[0];if(raw.indexOf('property/')===0){showView('property-page');const id=raw.slice(9);if(id)detail(id);return}showView(raw||'overview');if(raw==='overview')loadDashboard();if(raw==='properties')loadProperties();if(raw==='tenants')loadTenants();if(raw==='maintenance')loadMaintenance();if(raw==='payments'){loadPayments();loadOveruse()}if(raw==='imports')loadImports()}
function showView(id){document.querySelectorAll('.view').forEach(v=>v.hidden=v.id!==id);document.querySelectorAll('nav a').forEach(a=>{const href=a.getAttribute('href');a.classList.toggle('active',href==='#properties'?(id==='properties'||id==='property-page'):href==='#'+id)})}
function openProperty(id){showView('property-page');detail(id);const next='#property/'+id;if(location.hash!==next)location.hash=next}
function closeProperty(){location.hash='properties'}
function stat(label,value){return `<div class="stat"><span class="stat-label">${label}</span><strong class="stat-value">${value}</strong></div>`}
async function loadDashboard(){try{clearFail();const d=await api('/admin/dashboard');if(d.user){$('#user-name').textContent=d.user.firstName;$('#user-avatar').textContent=(d.user.firstName[0]+d.user.lastName[0]).toUpperCase()}$('#stats').innerHTML=stat('Properties',d.stats.properties)+stat('Total units',d.stats.units)+stat('Occupied',d.stats.occupied)+stat('Vacant',d.stats.vacant)+stat('Active tenants',d.stats.activeTenants)+stat('Open requests',d.stats.openMaintenance);$('#recent-maintenance').classList.remove('loading');$('#recent-maintenance').innerHTML=d.recentRequests.length?d.recentRequests.map(x=>`<div class="list-item"><div><strong>${esc(x.request.title)}</strong><small>${esc(x.user.firstName+' '+x.user.lastName)} - ${esc(x.property.name)} ${esc(x.unit.unitNumber)}</small></div><span class="badge ${x.request.status.toLowerCase()}">${esc(x.request.status.replace('_',' '))}</span></div>`).join(''):'<p class="muted">No maintenance requests yet.</p>';$('#recent-tenants').classList.remove('loading');$('#recent-tenants').innerHTML=d.recentTenants.length?d.recentTenants.map(x=>`<div class="list-item"><div><strong>${esc(x.user.firstName+' '+x.user.lastName)}</strong><small>${esc(x.user.email)}</small></div><span class="badge active">Tenant</span></div>`).join(''):'<p class="muted">No tenants yet.</p>';$('#overview-properties').classList.remove('loading');$('#overview-properties').innerHTML=d.propertyOverview.length?d.propertyOverview.map(x=>propertyCard(x)).join(''):'<p class="muted">No properties yet.</p>'}catch(e){['#stats','#recent-maintenance','#recent-tenants','#overview-properties'].forEach(selector=>{const node=$(selector);if(node){node.classList.remove('loading');node.innerHTML='<p class="muted">This section is temporarily unavailable. Refresh to try again.</p>';}});fail(e)}}
function propertyCard(x){const pct=x.unitCount?Math.round(x.occupied/x.unitCount*100):0;return `<article class="property-card"><h3>${esc(x.property.name)}</h3><p>${esc(x.property.addressLine1)}, ${esc(x.property.city)}, ${esc(x.property.state)}</p><p class="property-rent">${x.property.monthlyRent!=null?money(x.property.monthlyRent)+' / month':'Rent not set'}</p><div class="bar"><span style="width:${pct}%"></span></div><div class="property-meta"><span>${x.unitCount} units</span><span>${pct}% occupied</span></div></article>`}
async function loadProperties(){try{const rows=await api('/admin/properties');$('#property-list').classList.remove('loading');$('#property-list').hidden=false;$('#property-list').innerHTML=rows.length?rows.map(x=>`<article class="property-card" onclick="openProperty('${x.property.id}')"><h3>${esc(x.property.name)}</h3><p>${esc(x.property.addressLine1)}, ${esc(x.property.city)}, ${esc(x.property.state)} ${esc(x.property.zip)}</p><p class="property-rent">${x.property.monthlyRent!=null?money(x.property.monthlyRent)+' / month':'Rent not set'}</p><div class="bar"><span style="width:${x.unitCount?x.occupied/x.unitCount*100:0}%"></span></div><div class="property-meta"><span>${x.unitCount} units - ${x.occupied} occupied</span><button type="button" class="ghost" onclick="event.stopPropagation();openProperty('${x.property.id}')">Open</button></div></article>`).join(''):'<div class="panel"><p class="muted">No properties yet. Add your first property to get started.</p></div>'}catch(e){fail(e)}}
let lastProperty=null;
async function detail(id){try{clearFail();const pane=$('#property-detail');if(pane)pane.innerHTML='<div class="panel loading">Loading property...</div>';const d=await api('/admin/properties/'+encodeURIComponent(id));const chargeRows=await api('/admin/charges').catch(()=>[]);lastProperty=d;const mine=(Array.isArray(chargeRows)?chargeRows:[]).filter(x=>x.property&&x.property.id===id);const late=mine.filter(isLate);const dueAmt=mine.filter(x=>x.charge.status==='DUE'||x.charge.status==='OPEN').reduce((s,x)=>s+Number(x.charge.amount||0),0);const paidAmt=mine.filter(x=>x.charge.status==='PAID').reduce((s,x)=>s+Number(x.charge.amount||0),0);const leases=d.tenants||[];$('#property-detail').innerHTML=`<article class="panel"><button type="button" class="ghost" onclick="closeProperty()">Back to properties</button><div class="panel-head" style="margin-top:16px"><div><p class="eyebrow">Property</p><h2>${esc(d.property.name)}</h2><p class="muted">${esc(d.property.addressLine1)}, ${esc(d.property.city)}, ${esc(d.property.state)} ${esc(d.property.zip)}</p><p class="property-rent">${d.property.monthlyRent!=null?money(d.property.monthlyRent)+' / month':'Property rent not set'}</p></div><div class="property-actions"><button type="button" class="ghost" onclick="propertyRentForm('${id}')">Set rent</button><button type="button" class="primary" onclick="unitForm('${id}')">+ Add unit</button></div></div><div class="stats" style="margin-top:20px">${stat('Units',d.units.length)}${stat('Leases',leases.length)}${stat('Due now',money(dueAmt))}${stat('Past due',late.length)}${stat('Collected',money(paidAmt))}${stat('Open tickets',d.maintenance.length)}</div><div id="unit-editor"></div><h3 style="margin-top:26px">Rent</h3><div class="table-wrap"><table><thead><tr><th>Resident</th><th>Unit</th><th>Period</th><th>Amount</th><th>Status</th></tr></thead><tbody>${mine.length?mine.map(x=>`<tr class="${isLate(x)?'late-charge':''}"><td><strong>${esc(x.user.firstName+' '+x.user.lastName)}</strong><br><small>${esc(x.user.email)}</small></td><td>${esc(x.unit.unitNumber)}</td><td>${esc(x.charge.periodStart.slice(0,7))}<br><small>Due ${new Date(x.dueDate+'T00:00:00').toLocaleDateString()}${isLate(x)?' - Late':''}</small></td><td>${money(x.charge.amount)}</td><td>${badge(x.charge.status)}</td></tr>`).join(''):`<tr><td colspan="5" class="muted">${d.units.length?'No rent charges for this property yet. Assign a lease to a unit to generate rent.':'Add a unit, then assign a tenant lease to see rent and past due here.'}</td></tr>`}</tbody></table></div><h3 style="margin-top:26px">Units</h3><div class="table-wrap"><table><thead><tr><th>Unit</th><th>Bedrooms</th><th>Bathrooms</th><th>Status</th><th></th></tr></thead><tbody>${d.units.length?d.units.map(u=>`<tr><td><strong>${esc(u.unitNumber)}</strong></td><td>${u.bedrooms??'-'}</td><td>${u.bathrooms??'-'}</td><td><span class="badge ${u.status.toLowerCase()}">${u.status}</span></td><td><button type="button" class="ghost" onclick="unitForm('${id}','${u.id}')">Edit</button></td></tr>`).join(''):'<tr><td colspan="5" class="muted">No units yet.</td></tr>'}</tbody></table></div><h3 style="margin-top:26px">Residents</h3><div class="table-wrap"><table><thead><tr><th>Resident</th><th>Unit</th><th>Rent</th><th>Lease</th></tr></thead><tbody>${leases.length?leases.map(x=>`<tr><td><strong>${esc(x.user.firstName+' '+x.user.lastName)}</strong><br><small>${esc(x.user.email)}</small></td><td>${esc(x.unit.unitNumber)}</td><td>${money(x.lease.monthlyRent)}</td><td>${badge(x.lease.status)}</td></tr>`).join(''):'<tr><td colspan="4" class="muted">No residents on this property yet.</td></tr>'}</tbody></table></div></article>`;window.scrollTo({top:0,behavior:'smooth'})}catch(e){const pane=$('#property-detail');if(pane)pane.innerHTML='<article class="panel"><button type="button" class="ghost" onclick="closeProperty()">Back to properties</button><p class="muted" style="margin-top:16px">Could not open this property.</p></article>';fail(e)}}
function propertyRentForm(id){const value=lastProperty?.property?.monthlyRent??'';$('#unit-editor').innerHTML=`<form class="panel form-grid" onsubmit="savePropertyRent(event,'${id}')"><h3>Property monthly rent</h3><label>Rent amount<input name="monthlyRent" type="number" min="0" step="0.01" value="${esc(value)}" required></label><div class="form-actions"><button type="button" class="ghost" onclick="$('#unit-editor').innerHTML=''">Cancel</button><button class="primary">Save rent</button></div></form>`}
async function savePropertyRent(e,id){e.preventDefault();try{await api('/admin/properties/'+id,{method:'PATCH',body:JSON.stringify({monthlyRent:new FormData(e.target).get('monthlyRent')})});await detail(id)}catch(err){fail(err)}}
function unitForm(propertyId,id=''){const row=(lastProperty&&lastProperty.units||[]).find(u=>u.id===id)||{};$('#unit-editor').innerHTML=`<form class="form-grid" style="margin-top:18px" onsubmit="saveUnit(event,'${propertyId}','${id}')"><label>Unit number<input name="unitNumber" required value="${esc(row.unitNumber||'')}"></label><label>Bedrooms<input name="bedrooms" type="number" min="0" value="${row.bedrooms??''}"></label><label>Bathrooms<input name="bathrooms" type="number" step="0.5" min="0" value="${row.bathrooms??''}"></label><label>Status<select name="status"><option ${row.status==='VACANT'||!row.status?'selected':''}>VACANT</option><option ${row.status==='OCCUPIED'?'selected':''}>OCCUPIED</option><option ${row.status==='MAINTENANCE'?'selected':''}>MAINTENANCE</option></select></label><div class="form-actions"><button type="button" class="ghost" onclick="$('#unit-editor').innerHTML=''">Cancel</button><button class="primary">Save unit</button></div></form>`}
async function saveUnit(e,pid,id){e.preventDefault();try{const body=Object.fromEntries(new FormData(e.target));body.bedrooms=body.bedrooms?Number(body.bedrooms):null;body.bathrooms=body.bathrooms?Number(body.bathrooms):null;await api(id?`/admin/properties/${pid}/units/${id}`:'/admin/properties/'+pid+'/units',{method:id?'PATCH':'POST',body:JSON.stringify(body)});await detail(pid)}catch(err){fail(err)}}
async function loadTenants(){try{const rows=await api('/admin/tenants');$('#tenant-list').innerHTML=rows.length?rows.map(x=>`<tr><td><strong>${esc(x.user.firstName+' '+x.user.lastName)}</strong></td><td>${esc(x.user.email)}</td><td>${esc(x.tenant.phone||'-')}</td><td>${esc(x.property?.name||'-')} / ${esc(x.unit?.unitNumber||'-')}</td><td><span class="badge ${(x.lease?.status||'').toLowerCase()}">${esc(x.lease?.status||'No lease')}</span></td><td>${x.lease?.status==='ACTIVE'?'<span class="muted">Assigned</span>':`<button type="button" class="ghost" onclick="leaseForm('${x.tenant.id}')">Assign lease</button>`}</td></tr>`).join(''):'<tr><td colspan="6" class="muted">No tenants yet.</td></tr>'}catch(e){fail(e)}}
async function leaseForm(tenantId){try{const options=await api('/admin/lease-options');$('#lease-editor').innerHTML=`<form class="panel form-grid" onsubmit="saveLease(event,'${tenantId}')"><h2>Assign lease</h2><label>Unit<select name="unitId" required>${options.units.map(u=>`<option value="${u.id}">${esc(u.propertyName)} - ${esc(u.unitNumber)}</option>`).join('')}</select></label><label>Start date<input name="startDate" type="date" required></label><label>End date<input name="endDate" type="date"></label><label>Monthly rent<input name="monthlyRent" type="number" min="0.01" step="0.01" required></label><label>Security deposit<input name="securityDeposit" type="number" min="0" step="0.01"></label><label>Status<select name="status"><option selected>ACTIVE</option><option>PENDING</option></select></label><div class="form-actions"><button type="button" class="ghost" onclick="$('#lease-editor').innerHTML=''">Cancel</button><button class="primary">Assign lease</button></div></form>`}catch(e){fail(e)}}
async function saveLease(e,tenantId){e.preventDefault();try{await api('/admin/tenants/'+tenantId+'/lease',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});$('#lease-editor').innerHTML='';await loadTenants()}catch(err){fail(err)}}
async function loadMaintenance(){try{const rows=await api('/admin/maintenance');$('#maintenance-list').innerHTML=rows.length?rows.map(x=>`<tr><td><strong>${esc(x.request.title)}</strong><br><small>${esc(x.request.description)}</small></td><td>${esc(x.user.firstName+' '+x.user.lastName)}</td><td>${esc(x.property.name)} / ${esc(x.unit.unitNumber)}</td><td><span class="badge ${x.request.priority.toLowerCase()}">${x.request.priority}</span></td><td><select onchange="changeStatus('${x.request.id}',this.value)"><option ${x.request.status==='OPEN'?'selected':''}>OPEN</option><option ${x.request.status==='IN_PROGRESS'?'selected':''}>IN_PROGRESS</option><option ${x.request.status==='RESOLVED'?'selected':''}>RESOLVED</option><option ${x.request.status==='CLOSED'?'selected':''}>CLOSED</option></select></td><td><button type="button" class="ghost" onclick="maintenanceDetail('${x.request.id}')">Open</button></td></tr>`).join(''):'<tr><td colspan="6" class="muted">No maintenance requests yet.</td></tr>'}catch(e){fail(e)}}
async function changeStatus(id,status){try{await api('/admin/maintenance/'+id,{method:'PATCH',body:JSON.stringify({status})});await loadMaintenance();if($('#maintenance-detail').dataset.requestId===id)await maintenanceDetail(id)}catch(e){fail(e)}}
async function maintenanceDetail(id){try{const d=await api('/admin/maintenance/'+id);$('#maintenance-detail').dataset.requestId=id;$('#maintenance-detail').innerHTML=`<article class="panel" style="margin-top:20px"><div class="panel-head"><div><p class="eyebrow">Request details</p><h2>${esc(d.request.title)}</h2><p class="muted">${esc(d.user.firstName+' '+d.user.lastName)} - ${esc(d.property.name)} / ${esc(d.unit.unitNumber)}</p></div><span class="badge ${d.request.status.toLowerCase()}">${esc(d.request.status.replace('_',' '))}</span></div><p>${esc(d.request.description)}</p>${photoMarkup(d.request.photoUrl,d.request.title)}<h3>Timeline</h3><div>${d.comments.length?d.comments.map(x=>`<p><strong>${esc(x.user.firstName+' '+x.user.lastName)}</strong> <small>${new Date(x.comment.createdAt).toLocaleString()}</small><br>${esc(x.comment.body)}</p>`).join(''):'<p class="muted">No comments yet.</p>'}</div><form onsubmit="addAdminComment(event,'${id}')" class="form-actions"><input name="body" required maxlength="5000" placeholder="Add a management comment"><button class="primary">Add comment</button></form></article>`;$('#maintenance-detail').scrollIntoView({behavior:'smooth',block:'start'})}catch(e){fail(e)}}
async function loadPayments(){try{const status=$('#charge-status-filter')?.value;const rows=await api('/admin/charges'+(status?'?status='+encodeURIComponent(status):''));const body=$('#payments-list');body.classList.remove('loading');body.innerHTML=rows.length?rows.map(x=>`<tr class="${isLate(x)?'late-charge':''}"><td><strong>${esc(x.user.firstName+' '+x.user.lastName)}</strong><br><small>${esc(x.user.email)}</small></td><td>${esc(x.property.name)} / ${esc(x.unit.unitNumber)}</td><td>${esc(x.charge.periodStart.slice(0,7))}<br><small>Due ${new Date(x.dueDate+'T00:00:00').toLocaleDateString()}${isLate(x)?' - Late':''}</small></td><td>${money(x.charge.amount)}</td><td>${badge(x.charge.status)}</td><td>${x.charge.paidAt?new Date(x.charge.paidAt).toLocaleString():'-'}</td></tr>`).join(''):'<tr><td colspan="6" class="muted">No rent charges yet.</td></tr>'}catch(e){fail(e)}}

async function loadOveruse(){try{const [rows,tenants]=await Promise.all([api('/admin/overuse'),api('/admin/tenants')]);const active=tenants.filter(x=>x.lease?.status==='ACTIVE');const tenantSelect=$('#overuse-tenant');tenantSelect.innerHTML='<option value="">Select resident</option>'+active.map(x=>`<option value="${x.tenant.id}">${esc(x.user.firstName+' '+x.user.lastName)} — ${esc(x.property?.name||'')} / ${esc(x.unit?.unitNumber||'')}</option>`).join('');tenantSelect.onchange=()=>{const selected=active.find(x=>x.tenant.id===tenantSelect.value);$('#overuse-unit').innerHTML=selected?.unit?`<option value="${selected.unit.id}">${esc(selected.property.name)} / ${esc(selected.unit.unitNumber)}</option>`:'<option value="">Select resident first</option>'};const body=$('#overuse-list');body.classList.remove('loading');body.innerHTML=rows.length?rows.map(x=>`<tr><td><strong>${esc(x.user.firstName+' '+x.user.lastName)}</strong><br><small>${esc(x.user.email)}</small></td><td>${esc(x.property.name)} / ${esc(x.unit.unitNumber)}</td><td>${esc(x.usage.category)}</td><td>${esc(x.usage.periodStart)}</td><td>${money(x.usage.overageAmount)}</td><td>${badge(x.usage.status)}</td><td>${x.usage.status==='DUE'?`<button type="button" class="ghost" onclick="markOveruse('${x.usage.id}','PAID')">Mark paid</button> <button type="button" class="ghost" onclick="markOveruse('${x.usage.id}','WAIVED')">Waive</button>`:''}</td></tr>`).join(''):'<tr><td colspan="7" class="muted">No overuse charges recorded.</td></tr>'}catch(e){const body=$('#overuse-list');if(body){body.classList.remove('loading');body.innerHTML='<tr><td colspan="7" class="muted">Unable to load overuse charges.</td></tr>'}fail(e)}}
async function saveOveruse(e){e.preventDefault();try{await api('/admin/overuse',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});e.target.reset();$('#overuse-unit').innerHTML='<option value="">Select resident first</option>';await loadOveruse()}catch(err){fail(err)}}
async function markOveruse(id,status){try{await api('/admin/overuse/'+id,{method:'PATCH',body:JSON.stringify({status})});await loadOveruse()}catch(err){fail(err)}}
async function addAdminComment(e,id){e.preventDefault();try{await api('/admin/maintenance/'+id+'/comments',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});await maintenanceDetail(id)}catch(err){fail(err)}}
$('#show-property-form').onclick=()=>{$('#property-form').hidden=false};$('#cancel-property').onclick=()=>{$('#property-form').hidden=true};$('#property-form').onsubmit=async e=>{e.preventDefault();try{await api('/admin/properties',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});e.target.reset();e.target.hidden=true;await loadProperties()}catch(err){fail(err)}};$('#show-tenant-form').onclick=()=>{$('#tenant-form').hidden=false};$('#cancel-tenant').onclick=()=>{$('#tenant-form').hidden=true};$('#tenant-form').onsubmit=async e=>{e.preventDefault();try{await api('/admin/tenants',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});e.target.reset();e.target.hidden=true;await loadTenants()}catch(err){fail(err)}};$('#charge-status-filter').onchange=()=>loadPayments();$('#logout').onclick=async()=>{await api('/logout',{method:'POST'});location.href='/login'};window.openProperty=openProperty;window.closeProperty=closeProperty;window.detail=detail;window.changeStatus=changeStatus;window.maintenanceDetail=maintenanceDetail;window.addAdminComment=addAdminComment;window.saveOveruse=saveOveruse;window.markOveruse=markOveruse;window.unitForm=unitForm;window.saveUnit=saveUnit;window.propertyRentForm=propertyRentForm;window.savePropertyRent=savePropertyRent;window.leaseForm=leaseForm;window.saveLease=saveLease;window.addEventListener('hashchange',route);route();
let importDraft=null;const importFields={propertyName:'Property Name',addressLine1:'Address Line 1',addressLine2:'Address Line 2',city:'City',state:'State',zip:'ZIP',unitNumber:'Unit Number',tenantName:'Tenant Name',firstName:'First Name',lastName:'Last Name',email:'Email',phone:'Phone',leaseStart:'Lease Start',leaseEnd:'Lease End',monthlyRent:'Monthly Rent',leaseStatus:'Lease Status'};
async function loadImports(){try{const history=await api('/admin/imports/history');const body=$('#import-history-list');if(body)body.innerHTML=history.length?history.slice().reverse().map(x=>`<tr><td>${new Date(x.createdAt).toLocaleString()}</td><td>${esc(x.fileName)}</td><td>${x.rowsProcessed}</td><td>${x.createdCount}</td><td>${x.updatedCount}</td><td>${x.reviewCount}</td><td>${badge(x.status)}</td></tr>`).join(''):'<tr><td colspan="7" class="muted">No imports yet.</td></tr>'}catch(e){fail(e)}}
function renderImportMapping(){const headers=importDraft.headers;$('#import-mapping').innerHTML=Object.entries(importFields).map(([key,label])=>`<label>${label}${['propertyName','unitNumber','tenantName','leaseStart','monthlyRent'].includes(key)?' <strong class="required">required</strong>':''}<select data-import-field="${key}"><option value="">Do not map</option>${headers.map(h=>`<option value="${esc(h)}" ${importDraft.mapping[key]===h?'selected':''}>${esc(h)}</option>`).join('')}</select></label>`).join('')}
function currentImportMapping(){const mapping={};document.querySelectorAll('[data-import-field]').forEach(select=>{if(select.value)mapping[select.dataset.importField]=select.value});return mapping}
function renderImportPreview(){const counts=importDraft.counts;$('#import-counts').innerHTML=Object.entries({Rows:counts.rows,Properties:counts.properties,Units:counts.units,Tenants:counts.tenants,Leases:counts.leases,New:counts.new,Existing:counts.existing,Updates:counts.updates,Review:counts.review}).map(([key,value])=>`<span class="import-count"><strong>${value}</strong><small>${key}</small></span>`).join('');$('#import-preview-list').innerHTML=importDraft.preview.map(record=>`<tr><td>${record.rowNumber}</td><td>${esc(record.property?.name||'-')} / ${esc(record.unit?.unitNumber||'-')}</td><td>${esc(record.tenant?`${record.tenant.firstName} ${record.tenant.lastName}`:'-')}<br><small>${esc(record.tenant?.email||'')}</small></td><td>${record.lease?`${esc(record.lease.startDate)} · ${money(record.lease.monthlyRent)}`:'-'}</td><td>${badge(record.classification)}</td><td>${esc([...(record.issues||[]),...(record.warnings||[])].join(' ')||'—')}</td><td>${record.classification==='REVIEW'?`<label><input type="checkbox" data-import-skip="${record.rowNumber}"> Skip row</label>`:'Ready'}</td></tr>`).join('')}
async function refreshImportPreview(){try{const response=await api('/admin/imports/preview',{method:'POST',body:JSON.stringify({token:importDraft.token,mapping:currentImportMapping()})});importDraft={...importDraft,...response};renderImportPreview()}catch(e){fail(e)}}
$('#import-upload-form').onsubmit=async event=>{event.preventDefault();try{const file=$('#import-file').files[0];const form=new FormData();form.append('file',file);const response=await fetch('/admin/imports/upload',{method:'POST',body:form});const data=await response.json();if(!response.ok)throw new Error(data.error||'Unable to analyze spreadsheet.');importDraft=data;$('#import-workspace').hidden=false;$('#import-meta').textContent=`${data.fileName} · ${(data.fileSize/1024).toFixed(1)} KB · ${data.sheets.join(', ')} · ${data.headers.length} detected columns`;renderImportMapping();renderImportPreview();$('#import-report').href=`/admin/imports/report/${data.token}`;loadImports()}catch(e){fail(e)}};
$('#import-preview-button').onclick=refreshImportPreview;$('#import-save-template').onclick=()=>{if(importDraft)localStorage.setItem('tenant-portal-import-template',JSON.stringify(currentImportMapping()));};$('#import-confirm').onclick=async()=>{if(!importDraft)return;const review=importDraft.preview.filter(x=>x.classification==='REVIEW');if(review.length&&!confirm(`${review.length} row(s) require review. Import only valid rows and skip them?`))return;if(!confirm(`Import ${importDraft.preview.length-review.length} valid row(s) into this organization? Existing matches will be updated; no login credentials will be created.`))return;try{const skipRows=[...document.querySelectorAll('[data-import-skip]:checked')].map(x=>Number(x.dataset.importSkip));const result=await api('/admin/imports/confirm',{method:'POST',body:JSON.stringify({token:importDraft.token,skipRows})});alert(`Import complete. Created: ${result.result.created}. Updated: ${result.result.updated}. Skipped: ${result.result.skipped}. Needs review: ${result.result.review}.`);importDraft=null;$('#import-workspace').hidden=true;$('#import-upload-form').reset();loadImports()}catch(e){fail(e)}};
